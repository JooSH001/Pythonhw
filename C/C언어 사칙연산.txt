#include <stdio.h>

void main()
{
    int result = 0;
    
    int a, b, c;
    
    scanf("%d", &a);
    scanf("%d", &b);
    scanf("%d", &c);

    result = (((a + b) * c) - (a - b)) / c;
    
    printf("%d", result);
}
